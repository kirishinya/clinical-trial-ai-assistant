# 项目讲解稿（给完全不懂编程的临床医学生）

## 一句话怎么介绍这个项目

这是一个用虚构数据做成的临床试验辅助 Demo。它把 Protocol 阅读、受试者资料核对、Source 与 EDC 数据核查，以及 CRA 问题跟进串成一条可复核的工作流。程序负责重复性的整理和提醒，研究者负责最终医学判断。

## 1. 整个项目怎么运行

你双击“启动项目”后，Python 会启动一个本地网页。你在网页上点击按钮时，程序会读取项目里的模拟 PDF 或 CSV，然后执行对应步骤：

```text
用户点击 → Python 读取资料 → 规则或 AI 处理
→ 网页展示结果 → 人工复核 → 更新待办状态
```

它默认使用 Demo 模式，所以没有 API Key、没有网络也能完整演示。真实 AI 模式开启后，只有需要理解长文本或组织自然语言的步骤才调用大模型。

## 2. Python 在哪里使用

Python 是这个项目的“总调度员”。它负责：

- 启动 Streamlit 网页；
- 读取 PDF 和 CSV；
- 执行入排标准规则；
- 比较 Source 与 EDC；
- 生成 Worklist；
- 在真实 AI 模式下从服务器端调用大模型。

你可以把 Python 理解成把页面、数据、规则和 AI 连接起来的胶水。

## 3. Pandas 在哪里使用

Pandas 是 Python 里专门处理表格的工具，作用类似“可以自动执行规则的 Excel”。本项目用它：

- 读取患者、Source 和 EDC 的 CSV；
- 按 Patient ID 找对应记录；
- 比较日期、检验值、单位、用药和 AE 信息；
- 统计问题数量和风险等级；
- 生成可下载的核查结果和 CRA Worklist。

## 4. LLM 在哪里使用

LLM 就是大语言模型。本项目只让它处理比较适合语言理解的工作：

- 把 Protocol 长文本整理为结构化字段；
- 为规则初筛结果生成更容易阅读的辅助解释；
- 明确列出需要研究者确认的问题。

LLM 不负责精确数值比较，也不作最终医学决定。因为固定规则更稳定、可测试，医学判断必须由有资质的人负责。

## 5. Agent 是什么

在这个项目中，Agent 不是一个会自由行动的“AI 医生”。它是一条有顺序的自动化工作流：先读取输入，再调用合适的工具或规则，整理结果，最后交给人审核。

例如：

```text
Protocol → 提取标准 → 读取患者 → 规则匹配
→ AI 辅助解释 → 研究者审核
```

这说明你理解 Agent 的关键不是“全自动”，而是任务拆分、工具调用、状态传递和人工控制。

## 6. 哪些步骤是规则计算

- 年龄、ECOG、实验室阈值等初筛检查；
- Source 和 EDC 的日期、数值、单位比较；
- 重复、缺失、AE/SAE 信息检查；
- 风险等级和联系对象映射；
- Worklist 的优先级与状态管理。

规则计算的优点是结果稳定、可解释、容易测试。

## 7. 哪些步骤由 AI 完成

- 在真实 AI 模式下理解 Protocol 的自然语言；
- 提取信息时返回对应章节或原文依据；
- 把初筛规则结果整理成清楚的辅助说明；
- 对不确定内容标记“需要人工确认”。

Demo 模式不调用 AI，而是用预设结果完整演示业务流程。

## 8. 为什么不能让 AI 作最终入排判断

因为入排判断可能影响受试者权益和试验安全，必须结合完整病历、方案最新版本、研究者医学判断及现场情况。AI 可能误解文本、遗漏上下文或生成并不存在的信息。因此本项目只输出“可能匹配、需要人工确认、可能不匹配”，不输出“符合入组/不符合入组”。

这就是 Human-in-the-loop：AI 提高整理效率，人保留最终决定权和责任。

## 9. 面试官可能问什么

### Q1：为什么不把所有步骤都交给大模型？

建议回答：数值比较、缺失检查和阈值判断用确定性规则更稳定，也容易审计；LLM 更适合理解长文本和生成辅助解释。项目采用混合架构，把不同任务交给最合适的工具。

### Q2：怎么防止 AI 幻觉？

建议回答：要求每个 Protocol 结果附原文依据；缺少证据时必须标“需要人工确认”；限制结构化输出；关键步骤仍由研究者审核；AI 不修改源数据也不作最终判断。

### Q3：这个项目怎样体现你的 CRA / CRC 经验？

建议回答：功能围绕 Protocol 阅读、入排资料核对、Source 与 EDC 一致性检查、AE/SAE 关注和问题跟进设计；Worklist 还区分风险、联系对象与处理状态，贴近现场管理流程。

### Q4：Demo 模式和真实 AI 模式有什么区别？

建议回答：Demo 模式用固定规则和预设结果，演示稳定且不产生 API 成本；真实 AI 模式在服务器端调用模型处理长文本和辅助解释。两者共享同一业务流程和安全边界。

### Q5：API Key 为什么不会泄露？

建议回答：密钥只放在服务器端环境变量或被 Git 忽略的 `.env.local`，前端没有输入框也不会接收密钥，代码和 GitHub 仓库中都不保存真实 Key。

### Q6：数据质量检查会自动修正数据吗？

建议回答：不会。程序只读比较并产生问题清单，CRA / CRC 必须回到原始记录核对，再按正式流程处理 Query 或更正。

### Q7：如果要做成正式系统，还缺什么？

建议回答：需要验证和合规评估、权限与审计日志、数据加密、版本管理、电子签名、灾备、经过批准的系统接口，以及真实场景的性能和安全测试。

### Q8：你在项目里真正做了什么？

建议回答：我先把临床运营任务拆成可标准化步骤，定义 AI 与规则的边界，设计模拟数据和风险场景，再实现交互页面、规则核查、Agent 工作流和人工审核节点，并用自动测试检查结果。

## 10. 三分钟演示话术

“这是一个临床试验 AI 辅助 Demo，全部数据都是模拟的。首页展示两条 Agent 工作流。首先，Protocol 模块会提取试验信息并保留原文依据，无法确认的内容不会猜测。其次，受试者初筛通过确定性规则逐条核对，只输出三种初步状态，最终资格必须由研究者判断。然后，数据质量模块用 Pandas 只读比较 Source 和 EDC，识别日期、实验室、单位、用药及 AE/SAE 问题。最后，这些问题会进入 CRA Worklist，按风险排序并由人工更新状态。项目的核心价值是把重复工作标准化，同时把医学决定和数据修改权保留给人。”

## 你一定要记住的三句话

1. **这是辅助工具，不是医学决策系统。**
2. **确定性问题交给规则，语言理解交给 LLM。**
3. **所有关键结果都必须经过 Human-in-the-loop 人工审核。**
